import styled from 'styled-components';
import Inline from '../inline/inline';

const PillContainer = styled(Inline)`
  top: -0.75rem;
  left: 1rem;
  position: absolute;
`;

export default PillContainer;
